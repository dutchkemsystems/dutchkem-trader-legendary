# ── Database (SQLite for local dev — no PostgreSQL needed) ────
DATABASE_URL=sqlite:///C:/DUTCHKEM-TRADER-LEGENDARY-INTELLIGENCE-EDITION/backend/db.sqlite3

# ── Redis (unused in local dev — Django falls back to LocMemCache) ──
REDIS_URL=redis://localhost:6379/0

# ── Django ───────────────────────────────────────────────────
DJANGO_SECRET_KEY=local-dev-secret-key-not-for-production
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_SETTINGS_MODULE=config.settings
CORS_ALLOWED_ORIGINS=http://localhost:3000

# ── Celery (disabled for local dev) ─────────────────────────
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# ── MT5 Broker (simulation/paper mode) ──────────────────────
MT5_LOGIN=
MT5_PASSWORD=
MT5_SERVER=
MT5_PATH=
MT5_MAGIC=234000
MT5_SLIPPAGE=10
MT5_MODE=paper

# ── Frontend ────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8001
